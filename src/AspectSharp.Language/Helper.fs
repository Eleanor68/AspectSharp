namespace AspectSharp.Language

    module public Helper = begin

//        let rec public Implode (names, separator) = 
//            match names with
//            | [] -> ""
//            | [x] -> x.ToString()
//            | head :: tail -> head.ToString() + separator + (Implode(tail, separator))
//
//        let public ImplodeNamespace names = Implode(names, ".")

    end